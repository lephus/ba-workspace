# Utility / Shared Components

Shared building blocks for agents and workflows: activation templates, handler snippets, customize templates. Used by the agent compiler and installers when generating or validating agent definitions.

Ref: BMAD `src/utility/agent-components/` (activation-rules.txt, handler-*.txt, etc.).
