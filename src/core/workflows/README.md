# Core Workflows

Core-level workflows live here (e.g. help, list-tasks). Domain workflows (e.g. analyze-document) live in modules (e.g. `src/modules/ba-analysis/workflows/`).

Ref: BMAD `src/core/workflows/`.
